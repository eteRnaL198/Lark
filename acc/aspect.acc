aspect Foo {
  before(): execution(int foo(int, char)) {
    printf("aspect a=%d\n", a);
  }

  before(): execution(void bar(float)) {
    printf("before execution of bar");
  }
}

aspect Bar {
  before(): execution(void baz()) {
    printf("before execution of baz");
  }
}