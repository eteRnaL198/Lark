aspect Foo {
  around(): execution(int baz()) {
    printf("I'm Buzz Lightyear");
  }

  before(): execution(int foo(int, char)) {
    printf("aspect a=%d\n", a);
  }

  after(): execution(float bar(float)) {
    printf("aspect before execution of bar");
  }
}