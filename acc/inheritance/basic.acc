aspect BasicAspect {
  void func() {
    printf("basic aspect method\n");
  }
  
  /*
  after(): execution(int foo()) {
    this.func();
  }
  */
}

aspect Hoge {
  after(): execution(int hoge()) {
    if(1) {
      printf("hoge aspect method\n");
    }
  }
}