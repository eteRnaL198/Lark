aspect PureAspect {
  void func() {
    printf("pure aspect method\n");
  }
  
  after(): execution(int foo()) {
    this.func();
  }
}

aspect Hoge {
  after(): execution(int foo()) {
    printf("hoge aspect method\n");
  }
}