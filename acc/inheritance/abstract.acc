abstract aspect AbstractAspect {
  constructor(
    var,
    display,
    msg
  )

  abstract void method()
  
  before(): execution(int baz()) {
    this.var = 10;
    this.method();
    this.display(this.msg);
  }
}