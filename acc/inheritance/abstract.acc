abstract aspect AbstractAspect {
  constructor(
    var,
    display,
    msg,
  )

  abstract void func()

  before(): execution(int foo()) {
    this.var = 10;
    this.func();
    this.display(this.msg);
  }
}