aspect ConcreteAspect extends AbstractAspect {
  super(
    num,
    printf,
    "identifier param"
  )

  void func() {
    printf("func param\n");
  }
  
  after(): execution(int foo()) {
    printf("concrete aspect");
  }
}