aspect BasicAspect {
  void func() {
    printf("basic aspect");
    if(1) {
      printf(" method\n");
    }
  }
  
  after(): execution(int baz()) {
    this.func();
  }
}

aspect Hoge {
  after(): execution(int hoge()) {
    if(1) {
      this.func();
      if(1) {
        this.func();
      }
    }
  }
}